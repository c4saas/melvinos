# ─────────────────────────────────────────────────────────────
#  Atlas — Docker Compose
#  Services: app + postgres + claude-code
#
#  Usage:
#    cp .env.example .env   # fill in your keys
#    docker compose up -d
#
#  First run: navigate to http://localhost:3001 to set up your account
# ─────────────────────────────────────────────────────────────

services:

  # ── PostgreSQL ──────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    container_name: ${COMPOSE_PROJECT_NAME:-atlas}_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: atlasai
      POSTGRES_USER: atlas
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme_in_production}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - atlas_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U atlas -d atlasai"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  # ── Atlas App ───────────────────────────────────────────────
  atlas:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ${COMPOSE_PROJECT_NAME:-atlas}_app
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 3001
      AGENT_WORKSPACE_PATH: /app/workspace
      DATABASE_URL: postgres://atlas:${POSTGRES_PASSWORD:-changeme_in_production}@postgres:5432/atlasai
      QDRANT_URL: ${QDRANT_URL:-http://localhost:6333}
    ports:
      - "${HOST_PORT:-3001}:3001"
    volumes:
      - atlas_uploads:/app/uploads
      - agent_workspace:/app/workspace
      - claude_ssh:/var/ssh-keys-src:ro
    networks:
      - atlas_net
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://localhost:3001/api/auth/csrf-token').then(r=>r.ok?process.exit(0):process.exit(1)).catch(()=>process.exit(1))\""]
      interval: 30s
      timeout: 10s
      start_period: 60s
      retries: 3

  # ── Claude Code ─────────────────────────────────────────────
  claude-code:
    build:
      context: ./claude-code
      dockerfile: Dockerfile
    container_name: ${COMPOSE_PROJECT_NAME:-atlas}_claude
    restart: unless-stopped
    environment:
      ANTHROPIC_API_KEY: ""
    ports:
      - "${CLAUDE_SSH_PORT:-2222}:22"
    volumes:
      - claude_auth:/home/claude/.claude
      - agent_workspace:/workspace
      - claude_ssh:/root/.ssh
    networks:
      - atlas_net

volumes:
  postgres_data:
    driver: local
  atlas_uploads:
    driver: local
  agent_workspace:
    driver: local
  claude_auth:
    driver: local
  claude_ssh:
    driver: local

networks:
  atlas_net:
    driver: bridge
